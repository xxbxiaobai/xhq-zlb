import Vue from 'vue';
import { NavBar, Search, Tab,
         Tabs, Icon, Row, Col, 
         Uploader, Button, Grid, GridItem, TreeSelect } from 'vant';

Vue.use(NavBar)
.use(Icon)
.use(Search)
.use(Uploader)
.use(Button)
.use(Tab)
.use(Tabs)
.use(Row)
.use(Col)
// .use(Grid)
// .use(GridItem);
